import Foundation

enum AtlasAccessibilityID {
    enum Home {
        static let screen = "atlas.home.screen"
        static let yearSlider = "atlas.home.yearSlider"
        static let poiToggle = "atlas.home.poiToggle"
        static let selectedCountryButton = "atlas.home.selectedCountryButton"
        static let selectedPOIDismissButton = "atlas.home.selectedPOIDismissButton"
    }

    enum Settings {
        static let screen = "atlas.settings.screen"
        static let appearanceRow = "atlas.settings.appearanceRow"
        static let mapRow = "atlas.settings.mapRow"
        static let dataRow = "atlas.settings.dataRow"
        static let yearRangeLabelsToggle = "atlas.settings.map.yearRangeLabelsToggle"
        static let loadingIndicatorToggle = "atlas.settings.map.loadingIndicatorToggle"
    }
}
